---
name: Open Product Issues (Jason tracking)
description: Running list of open issues documented by Jason across all evaluation sessions
type: project
---

## Open Issues

| ID | Severity | Description | Status |
|----|----------|-------------|--------|
| JASON-001 | Critical | Sim-injected Gmail command messages never arrive in INBOX — daemon cannot process any user commands in live sim. Code fix (archiveFolder) implemented in f65c0ff but NOT activated: gmail_config.txt missing `archive-folder=[Gmail]/All Mail` | Open |
| JASON-002 | Critical | Gmail blocks outbound invite email to iCloud — DSN bounce received, invite never delivered | Open |
| JASON-003 | Major | Daemon has no mailer-daemon/postmaster filter — DSN bounces enter the message store | Open |
| JASON-004 | Minor | Verbose log dumps `[object Object]` for structured email headers (from, to, return-path, content-type) instead of human-readable strings. Partially addressed by logFmMessageSource() in f65c0ff — needs live run confirmation | Open |
| JASON-005 | Critical | gmail_config.txt is missing `archive-folder=[Gmail]/All Mail` — the archiveFolder fix for JASON-001 is inert without this config line | Open |
| JASON-006 | Critical | `$ invite --addfollower` silently adds the follower without sending any notification to the invitee. Alice receives posts from a stranger with no onboarding context. She has no mechanism to consent, unsubscribe, or understand why she is receiving mail. | Resolved (2026-06-15) |
| JASON-007 | Major | The `invite --addfollower` response (Message 3) had no HTML version. Every other daemon response in this flow rendered an HTML part. | Resolved (2026-06-15) |
| JASON-008 | Major | Welcome message (Message 1) included an `adduser:` mailto CTA link not present in the canonical spec. The link was removed, leaving only the `help:` link as specified. | Resolved (2026-06-15) |
| JASON-009 | Minor | Alice's Like/Comment links in the post notification point to Phil's address with no explanation. Architecturally correct but opaque to a first-time recipient. | Open |
| JASON-010 | Minor | The `adduser` display name convention is undiscoverable — welcome and help text do not explain how to set a name. | Open |
| JASON-011 | Minor | Message 4 (invite notification) has a hidden post-preview section. Correct behavior at invite time but the template is never re-used after a post exists. | Open |
| JASON-012 | Critical | new_follower_notification uses _displayName(email) for alice@test.com, producing "Alice L" instead of "Alice Johnson". The host receives incorrect information about who followed them. _displayName derives a fake last initial from the email domain — it should use the registered account name when one exists. | Resolved (2026-06-15) |
| JASON-013 | Minor | The plain `$ invite` flow sends a follow_response to alice confirming she is following, but sends no welcome-to-follower contextual message. Alice's experience as a new follower starts with just a terse confirmation. The `$ invite --addfollower` path sends the invite_addfollower_response; the `$ follow` response path sends nothing equivalent. | Open |

| JASON-014 | Major | Photo attachment silently dropped — post stored as `"type":"text"` when submitted with a photo. No error, no user feedback, no recovery path. Root cause was sim infrastructure: `run-live-sim.ts` was not passing photo bytes through to the daemon. | Resolved (2026-06-15) — fix: sim now loads attachment bytes from disk and uses `queueInboundMessage()` to preserve the in-memory photoAttachment object; post now stored as `"type":"image"` |
| JASON-015 | Major | Post notifications make no reference to a photo even when a photo was (or should have been) part of the post. Followers receive a notification indistinguishable from a text-only post. | Resolved (2026-06-15) — plain text body now includes `[Photo attached]`; HTML version includes `cid:post_photo` inline reference. See JASON-016 for follow-up on MIME structure. |
| JASON-016 | Major | HTML post notification references photo via `cid:post_photo` but sim output does not confirm photo bytes are embedded as a MIME part (multipart/related). If the message lacks the corresponding MIME attachment, HTML clients will render a broken image icon — worse than no image. Needs live raw MIME inspection to confirm the multipart/related structure is correct and image bytes are present. | Open |

| JASON-017 | Critical | `usermod --profile-pic` response (Sent/0003) and `new_post_notification` (Sent/0004) both reference `cid:profile_pic` in their HTML, but `LocalSimMailProvider.sendDraft()` constructs `SimpleMessageWithMessageId` without passing `draft.profilePicAttachment` or `draft.photoAttachment`. Neither attachment is embedded as a MIME part in the sim output. HTML clients will render broken image icons. The live path (`EmailMailProvider`) correctly embeds both via `inlineAttachments`. The sim path must do the same. | Resolved (2026-06-17) — in-memory object now carries profilePicAttachment; verified by LocalSimMailProvider.test.ts 7/7 passing |
| JASON-018 | Major | The `usermod --profile-pic` plain text response (Sent/0003.txt) says "Profile pic has been changed." but does not echo back any visual confirmation of the image. The user has no way to verify the correct photo was set. The HTML version shows a circular image thumbnail — the plain-text version should at minimum describe the filename that was stored (e.g. "Profile pic set to: linus.jpeg"). | Resolved (2026-06-17) — Sent/0003.txt now reads "Profile pic set to linus.jpeg." |
| JASON-019 | Major | The `new_post_notification` HTML (Sent/0004.html) has both a `<img src="cid:profile_pic">` path AND a fallback letter-avatar path, but both are always rendered simultaneously (no CSS display toggle that works in email clients). Email clients ignore `display:none` on `<table>` and `<tr>` elements. When no profile pic is set, the letter-avatar should be the only visible element; when one is set, the avatar block should be suppressed. | Resolved (2026-06-17) — Sent/0004.html now emits only `<img src="cid:profile_pic">` with no fallback letter-avatar table |
| JASON-020 | Minor | Inbox/0004.txt (the user's `$ usermod --profile-pic` command message) has no body other than the command line. The `linus.jpeg` attachment is referenced in the sim message spec but the inbox text file records no attachment header or filename. This is an artifact of the sim's text serialization format, but it means the stored inbox record gives no indication an image was sent, which could confuse debugging. Classified as latent persistence/infra concern, not an output defect — does not affect correctness of generated messages. | Open |

| JASON-021 | Minor | `SimMessageProvider._writeMessage()` serializes `From:` using `msg.from.toString()` only, dropping `fromName`. Live path (`EmailMailProvider`) constructs `"friendlymail" <addr>`. Daemon response Sent files all show bare email address in From header instead of `friendlymail <addr>`. Sim output does not faithfully represent the From display name that real recipients see. | Resolved (2026-06-17) — Sent files now show `From: friendlymail <ploden.postcards@gmail.com>` and `From: Phil Loden (via friendlymail) <...>` for invite-style messages |
| JASON-022 | Minor | `adduser_response` Create post CTA link pre-populates the email body with `Hello%2C%20world` (i.e. "Hello, world"). A real new user who clicks this will have awkward placeholder text in their compose window. The link body should either be blank or be a contextual example specific to the post format. | Resolved (2026-06-17) — mailto body is now empty (`body=`) |
| JASON-023 | Minor | Timestamp display in `new_post_notification` HTML (`Jun 17 at 2:31 PM`) is rendered via `toLocaleString('en-US', ...)` with no `timeZone` option, so it reflects the server's local timezone at render time. A server in UTC would show `8:31 PM`; a server in PDT shows `1:31 PM`. The displayed time is non-deterministic across deployments and misleading to users in other timezones. | Resolved (2026-06-17) — timestamp now renders with `timeZone: 'UTC'`; confirmed "Jun 17 at 8:42 PM" in output |

**Last updated:** 2026-06-17 — Fourth pass: JASON-021, JASON-022, JASON-023 all confirmed resolved; adduser_set_profile_pic flow APPROVED
